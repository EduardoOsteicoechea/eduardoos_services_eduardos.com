---
import PamphletRoutePage from "../../../components/Pamphlet/PamphletRoutePage.astro";

export function getStaticPaths() {
  return ["new", "open", "recent", "manage", "footers"].map((view) => ({
    params: { view },
  }));
}
---

<PamphletRoutePage />
