# eVoice TTS worker deps (spec 002 §5.7 / 044 / 069).
# System packages still required on the VPS: ffmpeg, tesseract-ocr, espeak-ng.
# piper-tts ships the `piper` binary used first; espeak-ng is the fallback.
# Super Premium PDF: pymupdf (preferred) rasterizes pages → OpenRouter vision.
# Fallback: poppler-utils (pdftoppm) on PATH.
#
# Env (see linux_sync.py docstring):
#   EVOICE_PYTHON, EVOICE_WORKER_SCRIPT, EVOICE_FAKE_TTS, EVOICE_MEDIA_ROOT,
#   EVOICE_JOB_TIMEOUT, EVOICE_PIPER_MODEL, OPENROUTER_API_KEY,
#   OPENROUTER_MODEL, OPENROUTER_VISION_MODEL
#
# Production: create a venv under backend/evoice-worker/.venv and point EVOICE_PYTHON at it.
# Local/tests: set EVOICE_FAKE_TTS=true so the Go API never shells out to this script.
python-docx>=1.1.0
pypdf>=4.0.0
pytesseract>=0.3.10
Pillow>=10.0.0
pymupdf>=1.24.0
piper-tts>=1.2.0
